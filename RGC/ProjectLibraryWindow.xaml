<Window x:Class="RGC.ProjectLibraryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RGC" Height="720" Width="960"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="Segoe UI">
    <Window.Resources>
        <Style TargetType="Button" x:Key="PrimaryBtn">
            <Setter Property="Background" Value="#89b4fa"/>
            <Setter Property="Foreground" Value="#1e1e2e"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#181825" CornerRadius="16" Margin="15" MouseDown="Window_MouseDown">
        <Grid Margin="32,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock x:Name="LibTitle" Text="Мои проекты" FontSize="26" FontWeight="Bold" Foreground="#cdd6f4" />
                    <TextBlock x:Name="ProjectCountText" Text="" FontSize="12" Foreground="#6c7086" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="NewProjectBtn" Content="＋ Новый проект" Style="{StaticResource PrimaryBtn}" 
                            Margin="0,0,12,0" Click="NewProject_Click"/>
                    <Button Content="✕" Width="32" Height="32" 
                            Background="Transparent" Foreground="#585b70"
                            BorderThickness="0" Cursor="Hand" FontSize="16"
                            Click="Close_Click"/>
                </StackPanel>
            </Grid>

            <!-- Empty state -->
            <Border x:Name="EmptyState" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Visible">
                <StackPanel HorizontalAlignment="Center">
                    <Border Width="72" Height="72" CornerRadius="20" Background="#1e1e2e" HorizontalAlignment="Center" Margin="0,0,0,16">
                        <TextBlock Text="📦" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock x:Name="EmptyTitle" Text="Нет проектов" FontSize="20" FontWeight="SemiBold" Foreground="#6c7086" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="EmptyDesc" Text="Создайте первый проект, чтобы управлять серверами" FontSize="13" Foreground="#585b70" HorizontalAlignment="Center" Margin="0,6,0,20"/>
                    <Button x:Name="EmptyBtn" Content="＋ Создать проект" Style="{StaticResource PrimaryBtn}" Click="NewProject_Click" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Cards -->
            <ScrollViewer x:Name="ScrollArea" Grid.Row="1" VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled" Visibility="Collapsed">
                <WrapPanel x:Name="ProjectTilesPanel" Orientation="Horizontal"/>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
